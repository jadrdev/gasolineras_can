org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Mantiene las bibliotecas nativas descomprimidas en el AAB/APK para
# garantizar el mapeo directo de memoria en dispositivos con tamaño de
# página de 16 kB (Android 15+). El valor por defecto de AGP 8.x es true,
# por lo que no es necesario (ni recomendable) forzarlo a false.
# android.bundle.enableUncompressedNativeLibraries=true
